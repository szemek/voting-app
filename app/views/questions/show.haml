.expanded.row
  .medium-8.large-8.columns
    %h1= question.title

    - question.options.each do |option|
      = form_for :option, url: question_option_vote_path(question, option) do |f|
        .expanded.row
          .medium-4.large-4.columns
            = option.title
          .medium-2.large-2.columns.end
            = f.submit('Vote', class: 'tiny button success hollow')

    %p
      = link_to 'See results', question_results_path(question), class: 'button secondary hollow'
    %p
      = link_to 'Back', root_path, class: 'button hollow'
